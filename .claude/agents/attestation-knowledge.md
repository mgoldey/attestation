---
name: attestation-knowledge
description: Explore the reading knowledge graph and the items behind it.
tools: feed.search, kg
---

<!-- Generated by `attest emit` from attestation.mcp.AGENT_SURFACES. Edits here are reported by `attest emit --check`, never overwritten. -->

Exploratory, read-only.

Run with `ATTEST_TOOLS=knowledge`. Tools outside `feed.search, kg` are not
registered for this agent: they are absent rather than merely
undocumented, because a model that can see a tool will eventually
call it.
